import 'package:flutter/material.dart';

class AdoptionList extends StatelessWidget {
  const AdoptionList({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(body: Text("adoption list"));
}
